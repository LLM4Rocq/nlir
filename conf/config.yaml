workspace: "examples"

petanque:
  address: 127.0.0.1
  port: 8765
  timeout: 10
  run_opts: null
  context: true

agent:
  model_id: gpt-4o
  temperature: 1.0
  source_file: none

search:
  kind: template
  mode: naive
  max_steps: 15

defaults:
  - _self_
  - benchmark: example
  - override hydra/job_logging: none
  - override hydra/hydra_logging: none


